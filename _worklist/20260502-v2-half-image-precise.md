# 2026-05-02 v2 half-image precise layout

## 目的
- 添付画像を基準に、v2 の縦長レイアウトを厳密に合わせる。
- 配置、並び順、余白、見出しの扱いを画像どおりに戻す。
- サイズ調整より先に、ボタンとパネルの位置関係を揃える。

## 対象
- 上部ヘッダー
- URL 行
- 操作列
- 本文カラム
- プレビューカラム

## 進め方
- まず現在の崩れを画像と突き合わせて戻す。
- その後、ボタンの並び順とアイコン種別を画像に寄せる。
- 最後に build を通して差分を閉じる。
- quick の色基調を確認し、v2 の背景・カード・ボタンからグラデーションを外して寄せる。

## 進捗
- [x] 作業リストを作成した。
- [x] 上部ヘッダーと URL 行の配置を画像に合わせる。
- [x] 操作列のアイコン種別と並び順を画像に合わせる。
- [x] 下段の本文カラムとプレビューカラムの見え方を画像に合わせる。
- [x] build で確認する。
- [x] URL 行と操作行を1つのパネルに統合した。
- [x] `PDF URL` をバッジではなく、入力行先頭の薄いインラインラベルとして扱うようにした。
- [x] URL 行を grid の 1 本線に戻し、`開始` が同じ行から落ちないようにした。
- [x] quick の配色に合わせて v2 の背景とボタン色をフラットにする。
- [x] 画像にある暖色系の強調ボタン色に合わせる。
- [x] 音量・速度ラベルの文字色を浮かないように揃える。
- [x] `開始` ボタンを暗色へ戻し、強調系ボタンを画像どおりの暖色へ寄せる。
- [x] アイコンがボタンに対して小さく見えないように、SVG の視覚サイズを上げる。
- [x] v2 の色を quick の実色に合わせ、独自色を外す。
- [x] 再生系アイコンの緑強調をやめ、quick の中立色に戻す。
- [x] カード背景の透過率を quick に合わせる。
- [x] 上段の再生操作を quick と同じ `current-session-action-button` 系に寄せる。
- [x] `再生成` の文字ボタンを外し、アイコンだけ残す。
- [x] 音量・速度を quick と同じ `audio-control-label` 構造に寄せる。
- [x] 音量・速度ブロックを再生ボタン列から少し離す。
- [x] 音量・速度ブロックをもう少しだけ再生ボタン列から離す。
- [x] 音量・速度のバーをラベル側へ少し寄せる。
- [x] 下段の 2 パネルが画面最下部まで届くように下余白を削る。
- [x] 下段の 2 パネルの親を flex にして、空状態でも高さが伝播するようにする。
- [x] 下段の 2 パネルを 50:50 の初期比率にし、境界をドラッグ可能にする。
- [x] プレビュー下部の `←前` / `次→` を削除する。
- [x] quick と同じ背景グラデーションに戻し、error カードの赤文字を外す。
- [x] `次へ進む` のアイコンを quick の形に合わせて、右の縦棒つきの形へ戻す。
- [x] 任意ページ移動の見た目に使っていた未使用スタイルを削除し、前後移動だけに絞る。
- [x] 自動送りの左に任意ページ移動の入力と移動ボタンを戻す。
- [x] 読み込み中の表示を v1 みたいなぐるぐる表示に戻す。
- [x] プレビュー側の読み込み中表示も v1 みたいな並びに合わせる。
- [x] 境界を端まで寄せたら片側パネルを非表示にする。
- [x] スマホ幅では説明/プレビューをタブ切り替えにする。
- [ ] スマホ幅のタブをもっとタブっぽくし、上段のはみ出しをなくす。
- [x] モバイル用の `開始` を右寄せにし、`自動送り` は前で改行する。
- [x] モバイルの最下段で `自動送り` を左、`お気に入り` を右に置く。
- [x] モバイルで `次 →` と `移動` を右端に寄せる。
- [x] モバイルのボタンとバーを横幅いっぱいに広げる。
- [x] モバイルの `移動` は同じ行に残し、ボタンを横長にする。
- [x] モバイルの音量・速度の数値を右端に揃える。
- [x] モバイルの再生系アイコンを少し大きくする。
- [x] モバイルの再生系アイコンをもう少し大きくする。
- [x] モバイルの再生系アイコンをさらに大きくする。
- [x] モバイルの再生系アイコンをもっと大きくする。
- [x] モバイルの再生系アイコンをかなり大きくする。
- [x] モバイルの音量・速度ブロックを横幅いっぱいに広げる。

## 完了
- 画像基準の横並びレイアウトへ戻した。
- 画像にないランダム操作やジャンプ入力は削除した。
- アイコン列を停止・再生・送り・再生成の見え方へ寄せた。
- URL 部分とボタン部分を別パネルに分けず、同じカード内の2段構成にした。

## 確認事項
- 画像内にない文言を出さない。
- 画像内のボタン配置を優先する。
- 縦長でも下部パネルが最下部まで伸びることを確認する。
