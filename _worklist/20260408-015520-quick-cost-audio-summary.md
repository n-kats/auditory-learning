# quick-auditory-learning コスト表示整理

## TODO
- [x] `先行実行` の行をコスト表示から外す。
- [x] `音声` を `音声生成` にして、音声の長さは合計の下に出す基盤を入れる。
- [x] 型チェックと backend の構文確認を通す。

## 決定事項
- `prefetch` はコスト表の表示対象から外す。
- `audio_duration_ms` をコストレスポンスに載せ、合計の下に `音声の長さ` を表示する。
- `paper_costs.audio_duration_ms` が欠ける場合は、フロントの音声メタデータから補完して表示する。

## 未決
- なし。

## 確認手順
- コスト表に `先行実行` が出ないことを確認する。
- `音声生成` が表の該当位置に出ることを確認する。
- 合計の下に `音声の長さ` が出ることを確認する。
