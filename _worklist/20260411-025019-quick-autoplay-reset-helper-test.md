# quick-auditory-learning autoplay reset helper test

## やること
- `resetAudio()` の自動再生既定値を helper に切り出してテストする

## 変更
- `resolveShouldAutoPlayAfterReset()` を追加
- `audioPlayback.test.ts` に helper の回帰テストを追加

## 確認
- frontend の `vitest` / `tsc`

