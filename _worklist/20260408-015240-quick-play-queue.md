# quick-auditory-learning 再生キュー修正

## TODO
- [x] 検索結果の `次に再生` を即時遷移ではなく queue 送信に変更する。
- [x] backend で queue を保持し、`next` 時に先頭から消費する。
- [x] queue 中の候補を UI で分かるように色付けする。
- [x] `キュー済み` の文言をやめて、色だけで状態を判定する。
- [x] `次に再生` を再クリックすると queue から外せるようにする。
- [ ] 型チェックと backend の構文確認を通す。

## 決定事項
- `select` は queue として扱い、即時に current paper を切り替えない。
- `next` は queue があればそれを優先し、なければ従来の検索結果からの自動進行を使う。
- queue 状態は文言では出さず、ボタンとカードの色で示す。
- `次に再生` はトグルとして動作し、queue 中なら解除する。

## 未決
- なし。

## 確認手順
- 検索結果で候補を queue に入れても current paper が切り替わらないことを確認する。
- 再生終了後に queue 先頭の論文へ移ることを確認する。
