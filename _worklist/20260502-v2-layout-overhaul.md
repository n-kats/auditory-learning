# 2026-05-02 v2 layout overhaul

- [x] v2 frontend を画像に合わせて 3 カラム前提へ再構成する。
- [x] 左サイドバーへ URL / 移動 / 再生操作を集約する。
- [x] 中央本文と右プレビューを独立したカラムに分ける。
- [x] 縦長 viewport で左操作群が上に詰まって見えるようにする。
- [x] build で破綻がないか確認する。

## 目的
- 既存の縦積みカード構成をやめ、画像のような密度の高いレイアウトへ寄せる。
- メモ欄などの補助要素は後回しにして、まず骨格を画像に合わせる。

## 追記
- 縦長 viewport を 1 カラムへ落とすのではなく、画像 2 のように 3 カラムのまま詰める方針へ修正した。
- 左サイドバーは細く、中央本文を主役に、右のプレビューを固定する構成へ寄せた。
- 低い viewport ではトップバー、カード余白、プレビュー高さをさらに削って押し込むようにした。
- 左サイドバーを 1 枚のパネルにまとめ、URL、ステータス、再生、移動を画像に近い密度で配置した。
- 中央と右は大きい見出しを外して、画像のようにメタ行と内容だけが立つ見え方へ寄せた。
- ランダムはアイコンではなく文字ボタンにして、画像で読める操作に戻した。
- さらに余白を削るため、左レールの大見出しを除去し、カード余白、行間、プレビュー高さをもう一段圧縮した。
- 画像1 は横長版、画像2 は縦長版として別レイアウトに分ける方針へ修正する。
- 縦長版では左の縦パネルをやめ、URL と操作帯を上部へ畳み、下に本文とプレビューを置く。
- 縦長版と横長版は `window.innerHeight > window.innerWidth` で切り替える。
- 上部の初期案内文は削除し、親コンテナを縦フレックス化して下部パネルが画面末尾まで伸びるようにした。
- 縦長版の上部から数値タイルを外し、操作帯をさらに圧縮した。
- 縦長版の下部 2 面は `align-items: stretch` で最下部まで伸びるようにした。
- 縦長版専用にボタン寸法を小さくし、画像 2 の密度へ寄せた。
- 縦長版の親子要素に `height: 100%` を追加し、解説が空でも下部面が最下部まで届くようにした。
- 縦長版のボタン寸法を戻し、画像 2 に近い見え方になるようにした。
- 縦長版では `PDF URL` の独立見出しと入力内ラベルを外し、入力行を画像 2 の密度に寄せた。
- 画像 1 を目標、画像 2 を現状として再整理し、縦長専用の別レイアウト分岐を廃止して画像 1 側の構造へ一本化した。
- URL 行は `PDF URL` の小さなバッジ、入力欄、開始ボタン、状態チップの 1 行にまとめ直した。
- アイコンボタンを画像 1 に近い大きさへ戻し、ページ移動の小ボタンと移動先入力も画像の密度に合わせて詰め直した。
