# quick-auditory-learning メモ同期整理

## TODO
- [x] メモの初期値を `GET /papers/{paper_id}/memo` で読む。
- [x] メモ保存を `PUT /papers/{paper_id}/memo` にする。
- [x] WebSocket の `memo/ws` で更新通知を受ける。
- [x] session WS の `memo_update` 経路を撤去する。
- [x] 変更後の型チェックを通す。
- [x] 切り替え時の `remoteValue` を初期化して、保存判定がぶれないようにする。

## 決定事項
- メモの初期値は HTTP で取り、保存も HTTP にする。
- メモの更新通知は `memo/ws` で受け、他端末の変更を即時反映する。
- session WS はメモ保存を扱わない。

## 未決
- なし。

## 確認手順
- 論文を切り替えたときにメモが DB から読まれることを確認する。
- メモ編集後に保存されることを確認する。
- 別タブや別端末で保存したメモが WS で反映されることを確認する。
