# quick-auditory-learning 開始ボタン色調整

## TODO
- [x] 「はじめから」の開始ボタンを他の白系ボタンに合わせる。
- [x] hover 色も周囲のボタンに揃える。
- [x] 変更後の型チェックを通す。

## 決定事項
- 開始ボタンは強い色を使わず、白基調の控えめなボタンにする。
- 共通の `.card button` から `start-button` を除外して、個別スタイルを優先させる。
- `再開` ボタンと同じ `width: 88px` のサイズ感にそろえる。
- コストの `再生単位` と同じ薄い緑を、開始ボタンの色として採用する。

## 未決
- なし。

## 確認手順
- 開始ボタンの色が `再開` や他の白系ボタンと違和感なく揃っていることを確認する。
