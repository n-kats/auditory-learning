# 2026-05-03 v2 session tab visibility

## 目的
- `現在のセッション` を、session が選ばれるまで表示しない。
- quick と同じく、start/favorites と current session の表示条件を分ける。

## 作業内容
- [x] topbar の `現在のセッション` を session 確立後のみ表示する。
- [x] build を確認する。

## 完了条件
- start/favorites ページでは `現在のセッション` が見えない。
- session 選択後にだけ `現在のセッション` が見える。
