# 20260410 fallback rule

## 目的
雑な fallback を追加しないルールを docs に明文化する。

## 完了
- `docs/spec/quick_auditory_learning.md` に fallback 方針を追記した。
- `docs/coding_rule/規約.md` に共通ルールとして fallback 方針を追記した。
- `フォールバックは基本的に導入しない。` を残した。
- 表現を `仕様に書いていない fallback は追加しない` に揃えた。

## 確認
- 仕様に書かれていない fallback は追加しない。
- 例外的な代替経路を入れるときは、対象と条件を先に書く。
